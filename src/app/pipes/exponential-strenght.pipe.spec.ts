import { ExponentialStrenghtPipe } from './exponential-strenght.pipe';

describe('ExponentialStrenghtPipe', () => {
  it('create an instance', () => {
    const pipe = new ExponentialStrenghtPipe();
    expect(pipe).toBeTruthy();
  });
});
